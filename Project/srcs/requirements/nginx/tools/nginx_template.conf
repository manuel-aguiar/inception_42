events {}

http {
	server
	{
		listen ${PORT_LISTEN_TLS_NGINX} ssl;
		listen [::]:${PORT_LISTEN_TLS_NGINX} ssl;

		server_name ${DOMAIN_NAME};

		ssl_certificate /etc/nginx/nginx-selfsigned.crt ;
		ssl_certificate_key /etc/nginx/nginx-selfsigned.key ;
		ssl_protocols TLSv1.2 TLSv1.3;

	}
}
